#+TITLE: looking-glass: profunctor lenses in emacs lisp

* DONE Fix critical and high findings from full-repo review (review.org)
CLOSED: [2026-07-25]
- [X] FCR-001: thread enclosing index through the indexed Star profunctor so
  ~lg-ifiltered~/~lg-indices~ receive real indices; composed-index tests.
- [X] FCR-002: type-driven JSON normalization dispatching on declared
  object/array types; reject ambiguous configurations at construction;
  round-trip tests for all accepted combinations.
- [X] FCR-003: ~lg-utf8~ accepts ASCII bytes (raw-byte-character check
  replaces the ~detect-coding-string~ gate); ASCII round-trip test.
- [X] FCR-004: ~save-match-data~ around regex span collection; regression
  test for caller match data.
- [X] FCR-005: ~lg>>~ composes outermost-to-innermost like ~lg-compose~,
  ~lg<<~ reversed; docstrings, manual, direction tests.

* DONE Fix medium findings from full-repo review (review.org)
CLOSED: [2026-07-25]
- [X] Bugs: FCR-006 (Either prisms total), FCR-007
  (~lg-unsupported-capability~ error; builder errors propagate), FCR-008
  (inert option removed).
- [X] Design: FCR-012 (~lg-icompose-with~), FCR-013 (composition-time
  validation), FCR-014 (asymmetry documented), FCR-015 (slice caveat),
  FCR-016 (MELPA recipes, ~-pkg.el~ removed, release branch strategy).
- [X] Feature gap: FCR-022 (~laws.org~ written).
- [X] Tech debt: FCR-024 (linear folds), FCR-025 (wrappers inlined),
  FCR-026 (public ~lg-traverse-list~), FCR-027 (heuristics gone),
  FCR-028 (regex constructors delegate), FCR-029/021 (CI byte-compile
  gate, tests first), FCR-030 (untested API covered; fixed latent
  ~lg-clone-affine~ bug), FCR-031 (define-before-use), FCR-032 (AGENTS.md
  purity policy).
- [X] Opportunistic: FCR-011, FCR-020, FCR-035, part of FCR-034.

* DONE Fix low/informational findings from full-repo review (review.org)
CLOSED: [2026-07-25]
- [X] Low bugs: FCR-009 (descriptive error for non-keyed lists), FCR-010
  (deletion removes duplicate keys).
- [X] Design: FCR-017 (aliases deleted), FCR-018 (~lg-just-o~ retagged),
  FCR-019 (classification documented).
- [X] Feature gap: FCR-023 (~lg-slice~ for vectors and strings).
- [X] Tech debt: FCR-033 (dead machinery deleted), FCR-034 (headers,
  deps), FCR-036 (caveat docstrings), FCR-037 (checkdoc clean + CI step),
  FCR-038 (~scripts/run-tests.sh~, install-info diagnostics), FCR-040
  (~json-available-p~ check, caveats documented).

* DONE Round-2 re-review and remediation (FCR-041..FCR-057, see review.org)
CLOSED: [2026-07-25]
- [X] FCR-041 fold crash on large containers (cat-tree monoid + 10k test).
- [X] FCR-042 ~lg-at~ raw-value silent deletion (setter validation).
- [X] FCR-043 load failure without native JSON (check deferred to use).
- [X] FCR-044 JSON sentinel collision validation.
- [X] FCR-045 ~lg-clone-affine~ multi-focus signals.
- [X] FCR-046 kind-pinned ~lg-plist-key~/~lg-alist-key~ +
  ~lg-plist-at~/~lg-alist-at~.
- [X] FCR-047 filtered-family law caveat; laws.org scoped.
- [X] FCR-048..056 contract docs, descriptive default methods, debt.

* DONE Compile reusable operation runners
CLOSED: [2026-07-27]
- [X] Add ~lg-compile~ for reusable unindexed and indexed update, collection,
  preview, view, and fold-map runners while preserving custom optics.
- [X] Test compiled runners against ordinary operations and document their
  operation-specific arguments and limitations.

* DONE Add looking-glass-json-schema package
CLOSED: [2026-07-27]
- [X] Define a generated optic-set API for JSON text and native parsed values.
- [X] Generate optics for object properties, homogeneous arrays, tuples,
  schema combiners, and local references across Jansson representations.
- [X] Add tests for raw and parsed inputs, every supported native object/array
  representation, escaped paths, optional properties, tuples, and references.
- [X] Add an Emacs manual, README examples, package recipe, and build/CI wiring.
- [X] Run tests, byte compilation, checkdoc, and manual builds.
- [X] Commit and push the completed package.

* DONE Replace JSON Schema path optics with compositional converters
CLOSED: [2026-07-27]
- [X] Define a whole-document converter API with reusable definition converters.
- [X] Compile scalar formats, objects, arrays, tuples, references, combiners, and
  tagged unions into recursively composed pure conversion optics.
- [X] Replace the existing JSON Schema tests with conversion, review,
  representation, recursion, union, and law-oriented coverage.
- [X] Rewrite the README and JSON Schema manual around external JSON parsing and
  whole-document semantic conversion.
- [X] Run tests, byte compilation, checkdoc, and manual builds.
- [X] Commit and push the replacement design.

* DONE Add the remaining standard JSON Schema semantic codecs
CLOSED: [2026-07-27]
- [X] Define semantic representations and canonical reviews for standard
  formats, documenting where Emacs has no dedicated native type.
- [X] Add codecs for time, duration, network addresses, URI/IRI references,
  email, hostnames, UUID, regex, JSON Pointer, relative JSON Pointer, and URI
  templates, plus Base64 content encoding.
- [X] Preserve parser-provided boolean/null/integer representations without
  duplicating conversions JSON already performs.
- [X] Add focused round-trip, validation, composition, and matching tests.
- [X] Update the README, manual, and law notes with representation choices and
  dialect/canonicalisation caveats.
- [X] Run tests, byte compilation, checkdoc, manual builds, and manual examples.
- [X] Commit and push the completed codec set.

* TODO Future work
- [ ] Forget-style short-circuit for ~lg-preview~ (folds currently visit
  all focuses; noted under FCR-024).
